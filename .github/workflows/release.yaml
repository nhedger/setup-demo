name: Release

on:
  workflow_dispatch:

jobs:
  test:
    name: Pre-release tests
    uses: ./.github/workflows/test.yaml

  changelog:
    name: Generate changelog
    needs: test
    outputs:
      tag: ${{ steps.version.outputs.tag }}
      version: ${{ steps.version.outputs.version }}
    permissions:
      contents: read
      pull-requests: read

    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          fetch-depth: 0
      - name: Set up git-cliff
        uses: taiki-e/install-action@43aecc8d72668fbcfe75c31400bc4f890f1c5853 # v2.83.2
        with:
          tool: git-cliff@2.13.0
      - name: Compute new version
        id: version
        run: |
          VERSION=$(git cliff --config .github/changelog.cliff.toml --bumped-version | tr -d 'v')
          if [ -z "$VERSION" ]; then
            echo "::error::git-cliff could not determine the next version."
            exit 1
          fi
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Generate full changelog
        run: |
          git cliff \
            --config .github/changelog.cliff.toml \
            --tag "${{ steps.version.outputs.tag }}" \
            --output CHANGELOG.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Generate release notes
        id: release-notes
        run: |
          git cliff \
            --config .github/release-notes.cliff.toml \
            --unreleased \
            --tag "${{ steps.version.outputs.tag }}" \
            --strip all \
            --output RELEASE_NOTES.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload changelog artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: changelog
          path: |
            CHANGELOG.md
            RELEASE_NOTES.md

  release:
    name: Release
    needs: [changelog]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
      - name: Update package version
        run: bun pm pkg set version=${{ needs.changelog.outputs.version }}
      - name: Install dependencies
        run: bun install
      - name: Bundle action
        run: bun run build
      - name: Download changelog artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: changelog
      - name: Set git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Create release commit
        run: |
          git add -f dist CHANGELOG.md package.json
          git commit -m "chore(release): ${{ needs.changelog.outputs.tag }}"
          git push origin main
      - name: Create version tags
        run: |
          PATCH="${{ needs.changelog.outputs.tag }}"
          MINOR="${PATCH%.*}"
          MAJOR="${PATCH%%.*}"
          git tag -af "$PATCH" -m "Release $PATCH"
          git tag -af "$MINOR" -m "$MINOR -> $PATCH"
          git tag -af "$MAJOR" -m "$MAJOR -> $PATCH"
          git push --force origin "$PATCH" "$MINOR" "$MAJOR" 
      - name: Create GitHub release
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1
        with:
          tag_name: ${{ needs.changelog.outputs.tag }}
          make_latest: true
          body_path: RELEASE_NOTES.md
